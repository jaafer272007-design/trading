"""Feature layer.

Pure, deterministic, causally-bounded transformations of a bar series. Every
module here (other than ``base``) implements one feature satisfying
:class:`features.base.Feature`, and every one is swept by
``tests/test_causality.py``.
"""
